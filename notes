TODO:
    [X] interval should be on goal
    [X] accumulator info should be on goal, get streak template from goal not streaks[0]
    [X] materialize
    [X] add new goal
    [X] new streak fills up until today
    [X] updateInterval form should be select from
    [X] delete goal
    [X] login
    [X] overlay when no goals
    [X] colors (do better materialize in general)
    [X] loading animation
    [X] refactor everything
    [ ] edit goal
    [ ] change goal color (color)
    [ ] tooltip hover streak to see goal (hover goal, elipses goal when 2long)
    [ ] handle errors
    [ ] do something with accumulator
    [ ] user should be able to pick color theme (reds, blues, greens, their color list will be greens)

    [ ] remove modal (what was this???)


refactor!!!!
    components:
        container
            header
                hook is catchy
                logout
                more nav?
            application
                streaks
                    [streak]
                goals
                    [goal]
            no-content
            loading
            edit goal:
                entry points:
                    new goal (application)
                    new goal (no-content)
                    edit goal (goal-card)

    pull me out of components pls:
        * config
        * api
        * util


``` javascript
const colors = [ 'red',
  'pink',
  'purple',
  'deep-purple',
  'indigo',
  'blue',
  'light-blue',
  'cyan',
  'teal',
  'green',
  'light-green',
  'lime',
  'yellow',
  'amber',
  'orange',
  'deep-orange',
  'brown',
  'grey',
  'blue-grey' ]
```
